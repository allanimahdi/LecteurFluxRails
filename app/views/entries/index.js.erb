$('#my-entries').append('<%= j render @entries %>');
<% if @entries.next_page %>
$('.pagination').replaceWith('<%= j will_paginate @entries %>');
<% else %>
$(window).off('scroll');
$('.pagination').remove();
<% end %>