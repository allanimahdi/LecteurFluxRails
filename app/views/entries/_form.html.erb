<%= form_with(model: entry, local: true) do |form| %>
  <% if entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :entry_title %>
  </div>

  <div class="field">
    <%= form.label :pulished %>
    <%= form.text_field :pulished, id: :entry_pulished %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content, id: :entry_content %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :entry_url %>
  </div>

  <div class="field">
    <%= form.label :lu %>
    <%= form.text_field :lu, id: :entry_lu %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
